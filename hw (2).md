# Python OOP Interview Questions

## Basic Concepts

1. What is Object-Oriented Programming (OOP)
2. What is the difference between a class and an object
3. What is a constructor in Python
4. What does the method `__init__` do
5. What is the purpose of the `self` keyword

## Classes and Objects

7. How do you create a class in Python
8. How do you create an object from a class
9. What is an instance attribute
10. What is a class attribute
11. What is the difference between instance variables and class variables
12. Can you access class variables from an instance

## Methods

13. What is the difference between:

* instance method
* class method
* static method

14. What decorator is used for a class method
15. What decorator is used for a static method
16. When would you use a staticmethod

## Encapsulation

17. What is encapsulation
19. What is the meaning of:

* `__variable`

20. how does the getter and setter work in python
21. What is name mangling

## Inheritance

22. What is inheritance
23. Why do we use inheritance
24. How do you create a child class
25. What is method overriding
26. What does `super()` do
27. What happens if the child class doesn't implement an abstract method of the parent

## Polymorphism

28. What is polymorphism
29. Give an example of polymorphism in Python
30. How can different classes use the same method name

## Special Methods

31. What are dunder methods
32. What does `__str__` do
33. What does `__repr__` do
34. What does `__len__` do
35. What is `__eq__`

## Small Coding Questions

### Question 1

Create a class `Dog` with:

* name
* age

Add a method `bark()` that prints "Woof"

### Question 2

Create a class `BankAccount` with:

* owner
* balance

Add methods:

* deposit
* withdraw

### Question 3

Create a parent class `Animal` with method `speak()`

Create two child classes:

* Dog
* Cat

Each class should print a different sound.

### Question 4

Add a `__str__` method to a class `Person` that returns:

```
Person(name=John, age=30)
```

Good luck 🚀

Submit email: **[pythonai200425+interview@gmail.com](mailto:pythonai200425+interview@gmail.com)**
